# Copy to apps/web/.env.local. Next.js does not load the repository-root .env.
TECHVAULT_API_URL=http://localhost:5078
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Base64 for exactly 32 random bytes; server-only, used to encrypt the 8-hour admin session cookie.
TECHVAULT_ADMIN_SESSION_SECRET=
